const CHUNK_SIZE_BLOCK = 25
const BLOCK_SIZE = 2
const CHUNK_SIZE = CHUNK_SIZE_BLOCK * BLOCK_SIZE
const WINDOW_WIDTH = window.innerWidth
const WINDOW_HEIGHT = window.innerHeight
const WATER_LEVEL = 400

const CONTROLS = {
    UP: 38,
    BOTTOM: 40,
    RIGHT: 39,
    LEFT: 37,
    JUMP: 32,
    MINE: 17
}